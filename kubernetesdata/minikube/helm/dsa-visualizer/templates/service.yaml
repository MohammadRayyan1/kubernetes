apiVersion: v1
kind: Service
metadata:
  name: {{ include "dsa-visualizer.fullname" . }}
  labels:
    {{- include "dsa-visualizer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "dsa-visualizer.selectorLabels" . | nindent 4 }}
